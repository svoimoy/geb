name: "types/golang"
version: "0.1"
about: "generator for golang types"
type: "types"


repeated:
  - name: type
    field: "."
    templates:
      - in: "default.go"
        out: "{{ pkg_path }}/{{name}}.go"
