name: "sample"
version: "0.1"
about: "Sample folder layouts and references"

output-dir: "output"
design-dir: "design"

dsl-config:
  paths:
    - "dsl"
    - "$HOME/.hofstadter/dsl"
  default:
    - dsl: sample

log-level: info
