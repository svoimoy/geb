type-list:
  name: "response-list"
  list:
    - name: version-response
      fields:
        - name: version
          type: string
    - name: delete-response
      fields:
        - name: total-deleted
          type: uint

