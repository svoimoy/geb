name: "count-service"
version: "0.1"
about: "A transaction counting service for AlchemyAPI services"

output-dir: "output"
design-dir: "design"

dsl-config:
  paths:
    - dsl
    - ~/.hofstadter/dsl
  default:
    - dsl: types
    - dsl: api

log-level: info
