name: "count-service"
version: "0.1"
about: "A key counting service"

output-dir: "output"
design-dir: "design"

dsl-config:
  paths:
    - "dsl"
    - "$HOME/.hofstadter/dsl"
  default:
    - dsl: types
    - dsl: api

log-level: info
