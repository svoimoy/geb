name: "api-example"
version: "0.1"
about: "A example api"

output-dir: "output"
design-dir: "design"

dsl-config:
  paths:
    - dsl
    - ~/.hofstadter/dsl
  default:
    - dsl: types
    - dsl: api

log-level: info
