api:
  name: serve
  about: serve templates and ETL pipelines with geb

  module: true

  config:
    host: "localhost"
    port: "1110"
    base-url: "/api/v1"

  prometheus-endpoints: true
  kubernetes-endpoints: true

