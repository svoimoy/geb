cli:
  name: geb

  commands:

    - name: gen
      usage: gen
      aliases:
        - geb
        - geberate
        - generate
        - g
      short: Generate a project.
      long: Generate a project from its working directory.

