pkg:
  name: "Unify"
  about: "the Hofstadter engine unify package"

  functions:

    - name: Unify
      args:
        - name: parent
          type: string
        - name: path
          type: string
        - name: parentPath
          type: string
        - name: designData
          type: "map:interface{}"
      return:
        - name: err
          type: error

    - name: unify
      args:
        - name: parent
          type: string
        - name: path
          type: string
        - name: pkgPath
          type: string
        - name: parentPath
          type: string
        - name: designData
          type: "interface{}"
      return:
        - name: err
          type: error
