name: "geb"
version: "0.1"
about: "The geb command-line tool for making profit."

output-dir: "contracrostipunctus"
design-dir: "design"
dsl-config:
  paths:
    - dsl
    - ~/.hofstadter/dsl
  default:
    - dsl: types
      gen:
        - golang
        - protobuf
    - dsl: cli

log-level: info

